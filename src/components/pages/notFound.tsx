/**
 * Renders a message indicating that the page does not exist.
 *
 * @return {JSX.Element} The JSX element displaying the message.
 */
const NotFound = () => {
  return <h1>This page does not exist.</h1>;
};

export default NotFound;
